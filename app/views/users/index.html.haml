- set_meta_tags title: t("user.users")

.row
  %div{class: center(xl: 4, lg: 6, md:8, sm: 10)}
    %section
      .header.text-center
        %h3.d-inline= t("user.users")
      .body
        %table{class: "table table-bordered table-sm mt-3"}
          %thead
            %tr
              %th= t("user.name")
              %th.text-center= t("user.admin")
          %tbody
            - @users.each do |user|
              %tr
                %td= link_to user.name, user
                %td.text-center= user.admin? ? t("symbol.tick") : t("symbol.cross")
      - if can?(:create, User)
        .footer= link_to t("user.new"), new_user_path, class: "btn btn-primary btn-sm"
