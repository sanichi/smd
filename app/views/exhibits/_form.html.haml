:ruby
  inputs = [
    [:name,     t("exhibit.name"),     :text, maxlength: Exhibit::MAX_NAME],
    [:location, t("exhibit.location"), :text, maxlength: Exhibit::MAX_LOCATION],
    [:link,     t("exhibit.link"),     :text],
    [:previous, t("exhibit.previous"), :check],
  ]
= render "utils/crud/form", object: exhibit, index: exhibits_path, inputs: inputs, title: title, center: Sni::Center.call(xl: 4, lg: 6, md: 8, sm: 10)
