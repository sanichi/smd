- flash.each do |type, msg|
  - if msg
    .flash.alert.alert-dismissable{class: type == "alert" ? "rails-alert" : "notice"}
      %button.close{"type" => "button", "data-bs-dismiss" => "alert"}= t("symbol.cross")
      = msg
