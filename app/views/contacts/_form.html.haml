:ruby
  inputs = [
    [:email, t("contact.email"), :text, maxlength: Contact::MAX_EMAIL],
    [:first_name, t("contact.first_name"), :text, maxlength: Contact::MAX_NAME],
    [:last_name,  t("contact.last_name"),  :text, maxlength: Contact::MAX_NAME],
  ]
= render "utils/crud/form", object: contact, index: contacts_path, inputs: inputs, title: title, center: center(xl: 4, lg: 6, md: 8, sm: 10)
